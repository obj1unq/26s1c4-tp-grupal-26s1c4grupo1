import wollok.game.*
import extras.* 
import level_1.*

program guitarHero {
        game.title("Guitar Hero")
        game.width(10)
        game.height(10)
        

        // Llamo al método init del nivel 1
        level1.init()
        
        game.start()
}
